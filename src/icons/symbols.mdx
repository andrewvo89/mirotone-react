import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';

import { Icon, symbolIcons } from '../';

<Meta title='Icons/Symbols' />

# Symbols

<IconGallery>
  {symbolIcons.map((name) => (
    <IconItem name={name}>
      <Icon key={name} name={name} />
    </IconItem>
  ))}
</IconGallery>
