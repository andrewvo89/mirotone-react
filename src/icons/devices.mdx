import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';

import { Icon, deviceIcons } from '../';

<Meta title='Icons/Devices' />

# Devices

<IconGallery>
  {deviceIcons.map((name) => (
    <IconItem name={name}>
      <Icon key={name} name={name} />
    </IconItem>
  ))}
</IconGallery>
