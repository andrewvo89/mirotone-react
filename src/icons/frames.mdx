import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';

import { Icon, frameIcons } from '../';

<Meta title='Icons/Frames' />

<IconGallery>
  {frameIcons.map((name) => (
    <IconItem name={name}>
      <Icon key={name} name={name} />
    </IconItem>
  ))}
</IconGallery>
