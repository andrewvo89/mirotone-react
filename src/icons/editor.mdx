import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';

import { Icon, editorIcons } from '../';

<Meta title='Icons/Editor' />

# Editor

<IconGallery>
  {editorIcons.map((name) => (
    <IconItem name={name}>
      <Icon key={name} name={name} />
    </IconItem>
  ))}
</IconGallery>
