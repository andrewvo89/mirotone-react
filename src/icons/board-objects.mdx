import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';

import { Icon, boardObjectIcons } from '../';

<Meta title='Icons/Board objects' />

# Board objects

<IconGallery>
  {boardObjectIcons.map((name) => (
    <IconItem name={name}>
      <Icon key={name} name={name} />
    </IconItem>
  ))}
</IconGallery>
