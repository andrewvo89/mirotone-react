import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';

import { Icon, objectIcons } from '../..';

<Meta title='Icons/Objects' />

<IconGallery>
  {objectIcons.map((name) => (
    <IconItem name={name}>
      <Icon key={name} name={name} />
    </IconItem>
  ))}
</IconGallery>
